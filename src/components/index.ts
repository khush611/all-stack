// barrel file for components - import from here instead of deep paths

// generic stuff like spinners, error messages
export { ErrorMessage, LoadingSpinner, Pagination } from './common'

// pokemon related ui pieces
export { TypeBadge, StatBar, StatsHeader, PokemonTable, PokemonDetail } from './pokemon'